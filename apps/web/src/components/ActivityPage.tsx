import { CheckCircle2, FileJson, ShieldCheck } from "lucide-react";
import { PROJECT_EVIDENCE } from "../data/projectEvidence";

export function ActivityPage() {
  return <main className="workspace feature-workspace activity-workspace"><header className="feature-heading"><div><h1>Activity & evidence</h1><p>Reference validation is shown separately from live API observations.</p></div><div className="read-only-mark"><ShieldCheck />Evidence only</div></header><section className="feature-panel"><div className="panel-title"><CheckCircle2 /><h2>Reference validation</h2></div><p className="evidence-disclosure">Project-level validation examples are not a live device event stream.</p><div className="activity-page-list">{PROJECT_EVIDENCE.activity.map((item) => <article key={item.title}><time>{item.time}</time><div><h3>{item.title}</h3><p>{item.detail}</p></div><span>{item.type}</span></article>)}</div></section><section className="feature-panel"><div className="panel-title"><FileJson /><h2>Rollback evidence classes</h2></div><p className="evidence-disclosure">Real recovery artifacts are operator-specific and remain outside the source repository.</p><div className="snapshot-page-list">{PROJECT_EVIDENCE.snapshots.map((item) => <article key={item.name}><FileJson /><div><h3>{item.name}</h3><p>{item.detail}</p></div><time>{item.time}</time></article>)}</div></section></main>;
}
