[package]
name = "keysmith-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
libc.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
thiserror.workspace = true

# hidraw is Linux-only but already validated there, and the hidapi crate needs
# libudev on Linux. Pull hidapi in only on the platforms that have no hidraw.
[target.'cfg(not(target_os = "linux"))'.dependencies]
hidapi.workspace = true
